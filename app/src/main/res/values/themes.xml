<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Coupleblog" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/light_purple</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- disable duplex clicks among buttons -->
        <item name="android:splitMotionEvents">false</item>
        <item name="android:windowEnableSplitTouch">false</item>

        <!-- material dialog theme -->
        <item name="materialAlertDialogTheme">@style/MaterialDialogTheme</item>

        <!-- Toolbar theme -->
        <item name="toolbarStyle">@style/ToolbarStyle</item>
        <!-- menu icon color in Toolbar -->
        <item name="colorControlNormal">@color/white</item>
        <!-- menu item text in Toolbar -->
        <item name="android:itemTextAppearance">@style/toolbarMenuItemTextStyle</item>

    </style>
</resources>